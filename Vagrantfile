
Vagrant.configure("2") do |config|

  config.vm.box = "sasha_testit"
 config.vm.provider "virtualbox" do |vb|
#хочу чтобы при старте запустился режим с графическим интерфейсом
    vb.gui = true
#хочу чтобы при старте было выделено вот столько памяти
    vb.memory = "1024"
   end

end
